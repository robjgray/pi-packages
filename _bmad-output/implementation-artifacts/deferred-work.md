# Deferred Work

Entries appended by the build workflow when an intent is split (SCOPE STANDARD,
multi-goal check). Each entry names a goal deferred from a prior session.

- source_spec: _bmad-output/external/pi-tanium-spec.md
  summary: pi-tanium hardening — rename `pi-path-recovery` → `pi-tanium` and ship Prong A (`edit`/`read`/`write` override: argument shape recovery, layered fuzzy match, `replaceAll`, similar-lines error, partial-read `cat -n` line numbering, write rewrite-loop guard) plus Prong B (`bash-gate.ts` `tool_call` extension + `pi-permission-system` deny config).
  evidence: Split from the pi-preface situational-activation goal during the `/bmad-build` multi-goal check. The pi-tanium spec (`_bmad-output/external/pi-tanium-spec.md`) bundles both, but they are independently shippable: pi-tanium is one package/PR, the pi-preface side-quest is a separate package with its own frozen-after-approval spec, Code Map, and acceptance criteria. User directed pi-preface to be built FIRST; pi-tanium is deferred to a later session. The spec's §0 testability note (always-on vs skill-conditional layering) is a testing concern, not a code dependency — pi-tanium's always-on layer is testable with no skill active.
- source_spec: `_bmad-output/implementation-artifacts/spec-pi-preface-situational-activation.md`
  summary: `matchReadToSkill` path matching has no normalization beyond `node:path.resolve` — symlinked skill dirs, case differences on case-insensitive filesystems (macOS default), and other non-canonical forms yield false negatives (a real `SKILL.md` read fails to flip the flag).
  evidence: Surfaced by the step-04 edge-case/blind-hunter review of the pi-preface situational-activation implementation. `conditions.ts:matchReadToSkill` does a raw `Set.has` against `resolve(ctx.cwd, path)` and the cached `filePath`; `resolve` does not resolve symlinks (`realpath`) or fold case. Low severity in practice (the model usually copies paths from pi-core's skill listing, so the strings match), but a real robustness gap. A future fix would canonicalize both sides (`fs.realpathSync`) and/or compare case-insensitively on case-insensitive platforms, with tests for the symlink and case-variant cases.
- source_spec: `_bmad-output/implementation-artifacts/spec-pi-preface-situational-activation.md`
  summary: `inject.ts` module header and `injectPreface` jsdoc are stale — they claim prepending applies to "user message (first generation) or a `toolResult` (tool rounds)", but `WRAPPABLE_ROLES = new Set(["user"])` excludes `toolResult` (tool rounds are handled by `before_provider_request` system-message insertion, not by `injectPreface`).
  evidence: Pre-existing doc staleness in `packages/pi-preface/src/inject.ts`, surfaced incidentally by the step-04 review. `inject.ts` was unchanged by this story (per spec), so the stale comment predates the change. Low priority doc fix — update the header/jsdoc to say `injectPreface` only wraps `user` messages; tool-round injection is a separate mechanism.
- source_spec: `_bmad-output/external/pi-tanium-spec.md`
  summary: pi-tanium Prong B — bash steering: a small `bash-gate.ts` `tool_call` extension in pi-tanium that catches residual file-mutation the `pi-permission-system` config can't see (heredoc bodies, `>`/`>>` redirects to source files), plus the drop-in `pi-permission-system` `deny`-with-rereason config (python stdin/heredoc, `python3 -c` file-IO, `sed -i`), plus the cross-tool `APPEND_SYSTEM.md` policy line.
  evidence: Split from Prong A during the `/bmad-build` multi-goal check on `_bmad-output/external/pi-tanium-spec.md` (second run). Prongs A and B are independently shippable: A is a tool-override package (edit/read/write hardening); B is mostly `pi-permission-system` config (a different package) + a small `tool_call` gate. The spec's §0 frames them as two prongs with different mechanisms; §4.3 phases the edit pure modules as Phase 1. Prong A is the headline (edit fails 51%); Prong B is the bash escape-hatch closure. Built second so pi-tanium v0.1.0 (hardened edit/read/write) lands before the bash-steering add-on. Prong B's deny-reason text references pi-tanium's hardened edit tool, so it benefits from A shipping first but does not require it at the config level.
- source_spec: `_bmad-output/implementation-artifacts/spec-pi-tanium-edit-hardening.md`
  summary: `edit-fuzzy.ts:findClosestRegion` returns an empty closest-region preview (`similarity: 0`) when `oldText` is wholly dissimilar to every file region — the not-found error then shows an empty region the model can't copy from.
  evidence: Surfaced by the step-04 edge-case-hunter review. Low severity (a wholly-dissimilar `oldText` means the closest region is meaningless anyway), but unhelpful — a future fix would fall back to a snippet of the file near `rankCandidates`'s best anchor so the model has *something* to read and re-issue from.
- source_spec: `_bmad-output/implementation-artifacts/spec-pi-tanium-edit-hardening.md`
  summary: The literal `120` is shared for two unrelated reasons — `REWRITE_LOOP_LARGE_LINES` (the write rewrite-loop guard threshold) and the read `promptGuidelines` "files >120 lines" hint — with no shared constant or comment linking them.
  evidence: Surfaced by the step-04 blind-hunter review. Cosmetic coupling; they can drift independently and the link is invisible to a future reader. Low priority — a shared constant or a clarifying comment would make the coincidence legible.